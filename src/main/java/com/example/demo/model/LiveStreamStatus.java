package com.example.demo.model;

/**
 * Tier-1 live streaming: the church broadcasts on YouTube Live and Klink only
 * tracks whether that broadcast is currently running.
 */
public enum LiveStreamStatus {
    LIVE,
    ENDED
}
